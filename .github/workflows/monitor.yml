name: Monitor mirror and score student repositories (SUSPENDED)

on:
  workflow_dispatch:

jobs:
  suspended:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "IJR monitoring suspended on 2026-08-04 to prevent GitHub Actions usage."
